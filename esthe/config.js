// Public configuration only. Never put credentials, emails, or spreadsheet IDs here.
window.ESTHE_CONFIG = Object.freeze({
  apiUrl: '', // New GAS API /exec URL, not the current HTML web-app URL.
  googleClientId: '', // Public OAuth client ID, not a client secret.
  timeoutMs: 45000
});
